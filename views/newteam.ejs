<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Team!</title>
</head>
<body>
    <form action="/createteam" method="POST">
    <h1>Pick a Scoring Format</h1>
    <input type="radio" name="Scoring" value="HPPR"> Half Point Per Reception<br />
    <input type="radio" name="Scoring" value="FPPR"> Full Point Per Reception<br />
    <input type="radio" name="Scoring" value="Standard"> Standard<br />
    <h1> Add Players to Your Team</h1>
            <% players.forEach(player => { %>
                <input type="checkbox" name="<%= player.Position %>" value="<%= player %>"><%= player.Name %>  <%= player.Position %><br />
                <% }) %>
        </form>
        </body>
</html>